import{wizardStore}from'../../nutrition_wizard/store/wizard.store.js';import{nutritionService}from'./nutrition.service.js';import{calculatorService,SPEED}from'./calculator.service.js';import{nutritionStore}from'../store/index.js';import{userService}from'../../user/services/user.service.js';import{i18nService}from'../../../core/i18n/i18n.service.js';class WizardService{async init(){wizardStore.reset();const currentBiometrics=nutritionStore.getWizardState()||{};const currentGoal=nutritionStore.getDietGoal()||{};wizardStore.hydrate(currentBiometrics,currentGoal);console.log('[WIZARD SERVICE] Sistema inicializado y borrador hidratado.');}
async saveStandardGoal(){const draft=wizardStore.getState();const speedMap={'safe':SPEED.SAFE,'normal':SPEED.NORMAL,'fast':SPEED.FAST};const input={weight:draft.weight,height:draft.height,age:draft.age,gender:draft.gender==='XX'?'female':'male',activityFactor:this._getActivityFactor(draft.activityLevel)};const appMode=i18nService.getPreferences().mode;const result=calculatorService.calculateMacros(input,draft.goalType,speedMap[draft.speed]||0,appMode);const finalGoal={...result,goalType:draft.goalType,formulaUsed:'harris_benedict',isCustom:false,activityFactor:this._getActivityFactor(draft.activityLevel)};this._syncBiometricsToCore(draft);await nutritionService.saveGoalOnly(finalGoal);console.log('[WIZARD SERVICE] Dieta Estándar calculada y guardada.');}
async saveCustomGoal(){const draft=wizardStore.getState();const targetKcal=parseInt(draft.targetKcal)||0;const interval=parseFloat(draft.interval)||0.05;const finalGoal={tmb:0,targetKcal:targetKcal,minKcal:Math.round(targetKcal*(1-interval)),maxKcal:Math.round(targetKcal*(1+interval)),protein:parseInt(draft.protein)||0,carbs:parseInt(draft.carbs)||0,fat:parseInt(draft.fat)||0,formulaUsed:'manual',goalType:'custom',isCustom:true,interval:interval};this._syncBiometricsToCore(draft);await nutritionService.saveGoalOnly(finalGoal);console.log('[WIZARD SERVICE] Dieta Experta guardada en Modo Dios.');}
_syncBiometricsToCore(draft){nutritionStore.setWizardState({gender:draft.gender,height:draft.height,age:draft.age,weight:draft.weight,activityLevel:draft.activityLevel});userService.updateBiometrics({weight:draft.weight,height:draft.height,age:draft.age,gender:draft.gender,activityLevel:draft.activityLevel});}
_getActivityFactor(level){const map={'sedentary':1.2,'light':1.375,'moderate':1.55,'active':1.725,'extreme':1.9};return map[level]||1.2;}}
export const wizardService=new WizardService();