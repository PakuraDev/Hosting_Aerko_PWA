const FACTORS={LB_TO_KG:0.453592,KG_TO_LB:2.20462,IN_TO_CM:2.54,CM_TO_IN:0.393701};class UnitService{toBase(value,inputUnit){const num=parseFloat(value);if(isNaN(num))return 0;const unit=(inputUnit||'').toUpperCase().trim();switch(unit){case'LB':case'LBS':return Number((num*FACTORS.LB_TO_KG).toFixed(2));case'KG':return Number(num.toFixed(2));case'IN':return Number((num*FACTORS.IN_TO_CM).toFixed(2));case'CM':return Number(num.toFixed(2));case'%':default:return Number(num.toFixed(2));}}
toDisplay(value,targetUnit){const num=parseFloat(value);if(isNaN(num))return 0;const unit=(targetUnit||'').toUpperCase().trim();switch(unit){case'LB':case'LBS':return Number((num*FACTORS.KG_TO_LB).toFixed(2));case'KG':return Number(num.toFixed(2));case'IN':return Number((num*FACTORS.CM_TO_IN).toFixed(2));case'CM':return Number(num.toFixed(2));case'%':default:return Number(num.toFixed(2));}}}
export const unitService=new UnitService();