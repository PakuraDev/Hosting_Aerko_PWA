export const FORMULAS={HARRIS_BENEDICT:'harris_benedict',CUSTOM:'custom'};export const GOALS={LOSE:'lose',MAINTAIN:'maintain',GAIN:'gain'};export const SPEED={SAFE:0.075,NORMAL:0.10,FAST:0.15};const MACRO_RATIOS={[GOALS.GAIN]:{p:1.8,f:1.0},[GOALS.MAINTAIN]:{p:1.6,f:0.75},[GOALS.LOSE]:{p:2.0,f:0.5}};class CalculatorService{calculateMacros(input,goalType,speedPercent=0,appMode='a'){let tmb=0;const w=parseFloat(input.weight);const h=parseFloat(input.height);const a=parseFloat(input.age);if(input.gender==='male'){tmb=88.362+(13.397*w)+(4.799*h)-(5.677*a);}else{tmb=447.593+(9.247*w)+(3.098*h)-(4.330*a);}
const factorBase=parseFloat(input.activityFactor);const interval=appMode==='b'?0.02:0.01;const factorMin=factorBase-interval;const factorMax=factorBase+interval;const tdeeMin=tmb*factorMin;const tdeeMax=tmb*factorMax;let modifier=1;if(goalType===GOALS.LOSE){modifier=1-speedPercent;}else if(goalType===GOALS.GAIN){modifier=1+speedPercent;}
const minKcal=Math.round(tdeeMin*modifier);const maxKcal=Math.round(tdeeMax*modifier);const targetKcal=Math.round(((tdeeMin+tdeeMax)/2)*modifier);const ratios=MACRO_RATIOS[goalType]||MACRO_RATIOS[GOALS.MAINTAIN];const proteinGrams=Math.round(w*ratios.p);const fatGrams=Math.round(w*ratios.f);const fixedKcal=(proteinGrams*4)+(fatGrams*9);const remainingKcal=targetKcal-fixedKcal;const carbGrams=remainingKcal>0?Math.round(remainingKcal/4):0;return{tmb:Math.round(tmb),minKcal:minKcal,maxKcal:maxKcal,targetKcal:targetKcal,protein:proteinGrams,fat:fatGrams,carbs:carbGrams};}}
export const calculatorService=new CalculatorService();