import{db}from'../../../core/db/index.js';import{cryptoService}from'../../../core/crypto/index.js';import{authService}from'../../auth/services/auth.service.js';class ExportService{async exportData(selectedVaults){if(!selectedVaults||selectedVaults.length===0){throw new Error("err_export_no_vaults");}
const config=await authService._getConfig();const isSecurityEnabled=config.security_enabled;const protectedVaults=config.protected_vaults||[];const exportPayload={};for(const vaultName of selectedVaults){const rawRecords=await db.getAll(vaultName);if(isSecurityEnabled&&protectedVaults.includes(vaultName)){if(!cryptoService.key){throw new Error("err_export_no_key");}
const decryptedRecords=[];for(const record of rawRecords){try{const decryptedData=await cryptoService.decrypt(record.data);decryptedRecords.push({id:record.id,data:decryptedData});}catch(error){console.warn(`[EXPORT] Imposible descifrar el registro ${record.id} de la bóveda ${vaultName}. Posible corrupción de datos.`,error);}}
exportPayload[vaultName]=decryptedRecords;}else{exportPayload[vaultName]=rawRecords;}}
const jsonString=JSON.stringify(exportPayload,null,2);const blob=new Blob([jsonString],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;const dateStr=new Date().toISOString().split('T')[0];a.download=`Aerko_Export_${dateStr}.json`;document.body.appendChild(a);a.click();setTimeout(()=>{document.body.removeChild(a);URL.revokeObjectURL(url);},150);return true;}}
export const exportService=new ExportService();