const CACHE_NAME='smart-training-v3';self.addEventListener('install',(event)=>{self.skipWaiting();console.log('🟢⚪ [Utillero] Instalado y calentando en la banda.');});self.addEventListener('activate',(event)=>{event.waitUntil(caches.keys().then((cacheNames)=>{return Promise.all(cacheNames.map((cache)=>{if(cache!==CACHE_NAME){console.log('🧹 [Utillero] Borrando tácticas viejas:',cache);return caches.delete(cache);}}));}).then(()=>self.clients.claim()));});self.addEventListener('fetch',(event)=>{if(event.request.method!=='GET')return;if(!event.request.url.startsWith('http'))return;if(event.request.url.includes('storage.googleapis.com/mediapipe-models/')){console.log('⚽ [Utillero] Dejo pasar este balón. Que lo baje y lo descargue el que sabe.');return;}
event.respondWith(fetch(event.request).then((networkResponse)=>{const clonedResponse=networkResponse.clone();caches.open(CACHE_NAME).then((cache)=>{cache.put(event.request,clonedResponse);});return networkResponse;}).catch(async()=>{console.log('📶 [Utillero] Sin conexión, buscando en el banquillo:',event.request.url);const cachedResponse=await caches.match(event.request);return cachedResponse||new Response('Offline - Jugador no convocado',{status:408});}));});self.addEventListener('message',async(event)=>{if(event.data&&event.data.type==='MEAL_NOTIFICATION_UPDATE'){const payload=event.data.payload;console.log('📻 [Utillero] Mensaje recibido por walkie-talkie:',payload);if(payload.notification){await programarAlarma(payload);}else{await cancelarAlarma(payload.id);}}});async function programarAlarma(payload){const[hours,minutes]=payload.time.split(':').map(Number);const now=new Date();let alarmTime=new Date(now.getFullYear(),now.getMonth(),now.getDate(),hours,minutes,0);if(alarmTime.getTime()<=now.getTime()){alarmTime.setDate(alarmTime.getDate()+1);}
if('showTrigger'in Notification.prototype){console.log(`⏰ [Utillero] Programando alarma offline para [${payload.label}] a las ${alarmTime.toLocaleString()}`);try{await self.registration.showNotification(payload.label,{body:`⏱️ ${payload.time}`,icon:'/assets/icons/icon-192.png',tag:payload.id,showTrigger:new TimestampTrigger(alarmTime.getTime())});}catch(error){console.error('❌ [Utillero] Error al programar la notificación:',error);}}else{console.warn('⚠️ [Utillero] El árbitro (Navegador) no permite la Notification Triggers API. No se puede programar en segundo plano.');}}
async function cancelarAlarma(id){const notifications=await self.registration.getNotifications({tag:id,includeTriggered:true});notifications.forEach(notification=>{notification.close();console.log(`🚫 [Utillero] Alarma cancelada para la comida: ${id}`);});}
self.addEventListener('notificationclick',(event)=>{event.notification.close();console.log('👆 [Utillero] El usuario ha tocado la notificación.');const timeString=event.notification.body.replace('⏱️ ','');const payload={id:event.notification.tag,label:event.notification.title,time:timeString,notification:true};programarAlarma(payload);event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then((windowClients)=>{for(let client of windowClients){if(client.url.includes('/nutrition')&&'focus'in client){return client.focus();}}
if(clients.openWindow){return clients.openWindow('/nutrition');}}));});self.addEventListener('notificationclose',(event)=>{console.log('👋 [Utillero] El usuario ha descartado la notificación sin abrir la app.');const timeString=event.notification.body.replace('⏱️ ','');const payload={id:event.notification.tag,label:event.notification.title,time:timeString,notification:true};programarAlarma(payload);});