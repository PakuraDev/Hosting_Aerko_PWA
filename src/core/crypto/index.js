const ALGORITHM_AES='AES-GCM';const ALGORITHM_KDF='PBKDF2';const HASH_ALGO='SHA-256';const KEY_LENGTH=256;const KDF_ITERATIONS=100000;class CryptoService{constructor(){this.key=null;}
async initSession(pin,salt){if(!salt){console.error('[CRYPTO] Security Violation: Attempted to derive key without Salt.');throw new Error('SECURITY_ERROR_MISSING_SALT');}
try{this.key=await this._deriveKey(pin,salt);console.log('%c[CRYPTO] Session Key Generated (RAM Only).','color: #BEFF00');return true;}catch(e){console.error('[CRYPTO] Critical Error deriving key:',e);return false;}}
lock(){this.key=null;console.log('%c[CRYPTO] Session Locked. RAM wiped.','color: orange');}
async encrypt(data){if(!this.key)throw new Error("Security Violation: No active session.");const encoder=new TextEncoder();const encodedData=encoder.encode(JSON.stringify(data));const iv=window.crypto.getRandomValues(new Uint8Array(12));const encryptedContent=await window.crypto.subtle.encrypt({name:ALGORITHM_AES,iv:iv},this.key,encodedData);return{iv:Array.from(iv),content:Array.from(new Uint8Array(encryptedContent))};}
async decrypt(encryptedPackage){if(!this.key)throw new Error("Security Violation: No active session.");const iv=new Uint8Array(encryptedPackage.iv);const content=new Uint8Array(encryptedPackage.content);try{const decryptedContent=await window.crypto.subtle.decrypt({name:ALGORITHM_AES,iv:iv},this.key,content);const decoder=new TextDecoder();return JSON.parse(decoder.decode(decryptedContent));}catch(e){console.warn('[CRYPTO] Decryption failed. Reason: Wrong PIN or Data Corruption.');throw new Error('DECRYPTION_FAILED');}}
async _deriveKey(pin,salt){const encoder=new TextEncoder();const keyMaterial=await window.crypto.subtle.importKey("raw",encoder.encode(pin),{name:ALGORITHM_KDF},false,["deriveKey"]);return window.crypto.subtle.deriveKey({name:ALGORITHM_KDF,salt:encoder.encode(salt),iterations:KDF_ITERATIONS,hash:HASH_ALGO},keyMaterial,{name:ALGORITHM_AES,length:KEY_LENGTH},true,["encrypt","decrypt"]);}}
export const cryptoService=new CryptoService();