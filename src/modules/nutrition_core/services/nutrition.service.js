import{db}from'../../../core/db/index.js';import{bus}from'../../../core/bus/index.js';import{nutritionStore}from'../store/index.js';import{calculatorService}from'./calculator.service.js';import{pantryService}from'./pantry.service.js';import{journalService}from'./journal.service.js';const VAULT_NAME='nutrition_vault';const GOAL_KEY='user_diet_goal';class NutritionService{constructor(){this.isReady=false;}
async init(){if(this.isReady)return true;console.log('[NUTRITION] Booting core systems...');try{const goalRecord=await db.get(VAULT_NAME,GOAL_KEY);if(goalRecord){nutritionStore.setDietGoal(goalRecord.data);}
await Promise.all([pantryService.init(),journalService.init()]);this._attachReactiveListeners();this.isReady=true;console.log('[NUTRITION] All systems green. GO.');return true;}catch(error){console.error('[NUTRITION] Critical Error during Init:',error);return false;}}
async saveGoalOnly(goal){nutritionStore.setDietGoal(goal);await db.put(VAULT_NAME,{id:GOAL_KEY,data:goal});console.log('[NUTRITION] Diet Goal updated successfully.');}
isCustomGoal(){const goal=nutritionStore.getDietGoal();return goal&&goal.isCustom===true;}
_attachReactiveListeners(){bus.on('USER_UPDATED',(userProfile)=>{this._recalculateDiet(userProfile);});}
async _recalculateDiet(userProfile){const currentGoal=nutritionStore.getDietGoal();if(!currentGoal||!currentGoal.targetKcal)return;if(currentGoal.isCustom){console.log('[NUTRITION] Dieta Custom detectada. Ignorando recálculo automático.');return;}
const input={weight:userProfile.weight,height:userProfile.height,age:userProfile.age,gender:userProfile.gender==='XX'?'female':'male',activityFactor:currentGoal.activityFactor||1.2};let speed=(currentGoal.goalType==='lose'||currentGoal.goalType==='gain')?0.10:0;const newResult=calculatorService.calculateMacros(input,currentGoal.goalType,speed);if(Math.abs(newResult.targetKcal-currentGoal.targetKcal)>10){const updatedGoal={...currentGoal,...newResult};await this.saveGoalOnly(updatedGoal);bus.emit('DIET_RECALCULATED',updatedGoal);console.log('[NUTRITION] Dieta recalculada automáticamente por cambio de peso.');}}}
export const nutritionService=new NutritionService();